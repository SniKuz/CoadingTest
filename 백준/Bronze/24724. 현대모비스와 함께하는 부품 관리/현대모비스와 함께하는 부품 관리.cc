#include <bits/stdc++.h>

using namespace std;
int t, n, a, b;


int main() 
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    cin >> t;
    for(int tc = 1; tc <= t; tc++)
    {
        cin >> n >> a >> b;
        int sumU = 0, sumV = 0;
        int u, v;
        for(int i = 0; i < n; i++)
        {
            cin >> u >> v;
        }
        cout << "Material Management " << tc << '\n';
        cout << "Classification ---- End!\n";
    }
    return 0;
}
